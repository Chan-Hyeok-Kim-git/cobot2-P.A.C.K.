# Cobot2 Voice 개발 이력 정리
정리 기준: 2026-07-15 ~ 2026-07-29 커밋 이력

## 한 줄 요약
재난 방송을 감지해 기본 비상 물품을 로봇에 전달하고, 추가 물품 음성 명령과 작업 완료 안내 방송까지 처리하는 ROS 2 음성 인터페이스를 완성했다.

## 날짜별 개발 흐름

| 날짜 | 주요 커밋 | 작업 내용 | 결과 |
|---|---|---|---|
| 07/15 | `6d24229`, `169054b` | Git 초기화, `.gitignore` 구성 | 프로젝트 저장소 기반 마련 |
| 07/21 | `a708162` | `MicController`, `WakeupWord`, `STT`, `KeywordParser`, `voice_command` 최초 구성 | 마이크 입력 → 재난 방송 감지 → STT → 키워드 처리의 기본 파이프라인 확보 |
| 07/22 | `6a735f9` ~ `51d2f69` | 패키지 재구성, `07_22 final` 정리 | ROS 2 패키지 형태와 기본 실행 구조 안정화 |
| 07/23 | `59ca058`, `6c15c07` | 2차 wake word 모델 `hello_rokey_8332_32.tflite` 추가, `wakeup_word_interrupt.py` 추가, README 작성 | 재난 방송 이후 추가 물품 명령을 받을 수 있는 기반 마련 |
| 07/24 오전 | `f2ca21a` | `robot_status_dummy_test.py` 추가, `voice_command` 상태 흐름 확장 | `/voice/command`, `/voice/additional_item`, `/robot/working` 연동 시작 |
| 07/24 오후 | `4655067`, `06cc41c`, `7acbe2d`, `b49c05c`, `82ebd49` | 고수준 흐름도 작성, 로봇 완료 신호 기반 종료 알고리즘, 즉시 빌드 가능 패키지와 README 개선 | 로봇 상태와 음성 노드가 함께 종료되는 예외 처리 완성 |
| 07/27 | `5363e7c` | `tts.py` 추가, 완료 안내 방송 흐름 추가 | 물품 작업 완료 후 음성 안내를 재생하는 기능 추가 |
| 07/29 | `0ab4a18` | 물품 리스트 수정, 종료 안내 방송 포함 `1.0` 패키지 정리 | `cobot2_voice_07_29_final` 최종 버전 생성 |

## 핵심 기술 결정

### 1. STT: Faster-Whisper 선택

- 비교 대상: OpenAI Whisper API, Gemma 4 E2B, Faster-Whisper(Base), SenseVoice-Small
- Faster-Whisper는 4개 재난 문장(지진·홍수·화재·공습)에서 모두 재난명을 추출했다.
- 평균 처리 시간은 약 0.49초였다.
- SenseVoice-Small은 더 빨랐지만 공습 문장에서 최종 키워드 추출 실패가 있어, 정확도와 응답 시간의 균형을 기준으로 Faster-Whisper를 채택했다.
- 최종 코드에서는 로컬 `small` 모델을 CPU `int8` 설정으로 사용한다.

### 2. 키워드 추출: LLM 제거

- 처리 대상이 `지진·홍수·화재·공습` 및 미리 정한 물품 목록으로 제한된다.
- STT 문장에 필요한 단어가 직접 포함되므로, 문맥을 생성·추론하는 LLM 호출이 필요하지 않다.
- `KeywordParser`가 허용 목록에 대한 문자열 포함 여부를 검사하도록 구성했다.
- 결과: API 지연·비용·출력 형식 오류를 줄이고, 항상 같은 입력에 같은 결과를 반환한다.

### 3. Wakeup1: 재난 방송 전용 모델 제작

1. CLOVA Dubbing으로 재난 방송 문장 음성 데이터를 준비했다.
2. Teachable Machine에서 `재난 방송`과 `배경 소음` 클래스를 학습했다.
3. 학습 모델을 `soundclassifier_with_metadata.tflite`로 배포했다.
4. `WakeupWord`는 재난 확률이 기준값을 넘을 때만 STT를 시작한다.

### 4. Wakeup2와 로봇 상태 연동

- 기본 물품 목록 발행 뒤 `/robot/working=True`를 수신하면 2차 wake word 단계로 전환한다.
- Wakeup2는 추가 물품 하나를 인식해 `/voice/additional_item`으로 발행한다.
- `/robot/finished=True`는 언제든 우선 처리한다.
- 완료 신호를 받으면 wake word timer를 멈추고 `STATE_WAIT_FINISHED`로 전환해 더 이상 음성을 받지 않는다.

### 5. 완료 안내 방송 후 종료

1. `/robot/finished=True` 수신
2. 현재 재난 유형에 맞는 안내 대본 선택
3. OpenAI TTS로 음성 생성
4. `pygame.mixer.music.get_busy()`로 재생 완료까지 대기
5. `rclpy.shutdown()`으로 노드 종료

## 최종 동작 흐름

```text
Wakeup1(재난 방송)
  → Faster-Whisper STT
  → 재난명 직접 추출
  → /voice/command: 기본 물품 목록 발행
  → /robot/working=True
  → Wakeup2(추가 물품)
  → Faster-Whisper STT
  → /voice/additional_item: 물품 1개 발행
  → /robot/finished=True
  → 재난별 완료 안내 TTS 재생
  → 재생 완료 후 VoiceCommandNode 종료
```

## 최종 패키지 구성

```text
cobot2_voice_07_29_final/
├── cobot2_voice/
│   ├── voice_command.py            # 전체 상태 흐름과 ROS topic 처리
│   ├── wakeup_word.py              # 1차 재난 방송 감지
│   ├── wakeup_word_interrupt.py    # 2차 추가 물품 wake word 감지
│   ├── stt.py                      # Faster-Whisper STT
│   ├── get_keyword.py              # 재난·물품 직접 추출
│   ├── tts.py                      # 완료 안내 방송 재생
│   ├── MicController.py            # 마이크 stream 제어
│   └── robot_status_dummy_test.py  # 로봇 상태 연동 테스트
└── resource/
    ├── soundclassifier_with_metadata.tflite
    └── hello_rokey_8332_32.tflite
```

## 대표 커밋

- `a708162` — 1차 음성 인식 파이프라인 시작
- `59ca058` — 2차 wake word 및 추가 물품 기능 기반
- `06cc41c` — 로봇 완료 신호 기반 노드 종료 알고리즘
- `5363e7c` — 완료 안내 방송(TTS) 추가
- `0ab4a18` — 1.0 최종 패키지
